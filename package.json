{
  "name": "pngcompressor",
  "version": "0.1.0",
  "description": "fast png compressing using oxipng",
  "main": "index.node",
  "scripts": {
    "build": "cargo-cp-artifact -nc index.node -- cargo build --release --message-format=json-render-diagnostics",
    "install": "npm run build",
    "test": "cargo test"
  },
  "author": "yashLadha",
  "license": "ISC",
  "devDependencies": {
    "cargo-cp-artifact": "^0.1"
  }
}
